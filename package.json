{
  "name": "kenya-address-widget",
  "version": "1.0.0",
  "description": "Embeddable customer address collection widget with Nairobi stage autocomplete.",
  "type": "module",
  "scripts": {
    "build:data": "node scripts/buildStages.mjs",
    "serve:widget": "http-server widget -p 5000 -c-1",
    "serve:demo": "http-server . -p 5500 -c-1",
    "prepare:public": "rm -rf public && mkdir -p public && cp demo/index.html public/index.html && cp widget/address-widget.js public/address-widget.js && cp widget/stages.json public/stages.json"
  },
  "keywords": [
    "nairobi",
    "address",
    "widget",
    "kenya",
    "open-source"
  ],
  "author": "Brian Ndeto",
  "license": "MIT",
  "dependencies": {
    "csv-parse": "^6.1.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
